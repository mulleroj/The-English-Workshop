[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback - všechny cesty přesměrovat na index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
